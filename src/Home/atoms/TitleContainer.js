import styled from 'styled-components';

const TitleContainer = styled.div`
    position: relative;
    width: 100%;
    text-align: center;
    background: #ddd;
    padding-top: 0.75em;
`;

export default TitleContainer;